EXTRACTION_SCHEMA: |
  {
    "bonds": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "cusip_no": {"type": "string"},
          "call_date": {"type": "string"},
          "redemption_reason": {"type": "string"},
          "maturity_date": {"type": "string"},
          "current_principal": {"type": "string"},
          "called_amount": {"type": "string"},
          "outstanding_amount": {"type": "string"}
        }
      }
    }
  }
INSTRUCTIONS: |
  Extract all CUSIP numbers and their corresponding bond attributes from the document.
  CUSIP numbers are typically 9-character alphanumeric codes.
  For each CUSIP found, extract all available attributes from nearby table rows or cells.
  If an attribute is not found or unclear, use null.
  Preserve exact values and dates as they appear in the source.
  Handle multiple CUSIP entries in a single document.
AGENT_BRICKS_ENDPOINT: "kie-966147c4-endpoint"
